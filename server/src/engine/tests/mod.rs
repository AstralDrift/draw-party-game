//! Engine unit tests — split by domain so the suite can grow without a monolith.

mod helpers;

mod actions;
mod drawing_validation;
mod host;
mod names;
mod phases_reconnect;
mod prompts;
mod reactions;
mod scoring;
mod settings;
mod show;
