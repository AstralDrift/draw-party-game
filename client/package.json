{
  "name": "draw-party-client",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc -p tsconfig.json && vite build",
    "e2e": "playwright test -c e2e/playwright.config.ts",
    "e2e:couch-loop": "playwright test -c e2e/playwright.config.ts --grep \"four phones complete two rounds|simulated on-screen keyboard|a locked phone vote|phone, Fire tablet, and iPad drawing|phone drawing screen prioritizes canvas|TV progress names submitted|overlay keeps|drawing interrupted by reconnect stays retryable|mixed motion preferences keep Continue|reduced-motion eight-player results|one-round finale renders podium|TV lobby gives room code|manual join keyboard|QR join confirms|late join mid-drawing\"",
    "e2e:install": "playwright install chromium",
    "e2e:tv": "playwright test -c e2e/playwright.config.ts --grep \"TV layout gate\"",
    "e2e:tvbro": "playwright test -c e2e/playwright.config.ts --grep \"TV Bro pixel preview\"",
    "review:tv": "TV_REVIEW=1 playwright test -c e2e/playwright.config.ts --grep \"TV layout gate\"",
    "review:tvbro": "TV_REVIEW=1 playwright test -c e2e/playwright.config.ts --grep \"TV Bro pixel preview\"",
    "review:tvbro:device": "bash ../scripts/tvbro-device/run.sh",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest"
  },
  "dependencies": {
    "lucide": "^1.24.0",
    "qrcode": "^1.5.4",
    "react": "^19.2.8",
    "react-dom": "^19.2.7"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/qrcode": "^1.5.5",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.3",
    "happy-dom": "^20.10.6",
    "react-grab": "^0.1.48",
    "react-scan": "^0.5.7",
    "typescript": "^7.0.2",
    "vite": "^8.1.5",
    "vitest": "^4.1.10"
  }
}
